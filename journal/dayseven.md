# Day Seven

Today was by far the easiest day for me. I was able to one shot both parts.
I only spent about five minutes thinking about the problem. I was able to
modify my solution from part one to part two easily, simply converting
`beams` and derivatives from a `map[int]bool` to a `map[int]int`.
