# Day One

Today felt mostly like a `Go` learning curve:

- The modulus operator doesn't work the way I expect (it's not a true modulus, 
  just a remainder).
- I had to implement my own floor div so that it would work as expected with negative 
  numbers.

The rest was pretty easy math.
